const { StyleSheet } = require("react-native");
const { secondary_color } = require("./Common");

export const styles = StyleSheet.create({
    playerCard:{
        backgroundColor: secondary_color,
        height: '100%',
        padding: 40,
    }
})